<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/linox_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="58dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:gravity="center_vertical"
        android:background="@color/linox_panel">

        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="LinOx Mobile 0.9.1"
            android:textColor="@color/linox_accent"
            android:textSize="20sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Linux runtime"
            android:textColor="@color/linox_muted"/>
    </LinearLayout>

    <FrameLayout
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:background="@color/linox_panel">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:padding="8dp">

            <Button android:id="@+id/btnTerminal" android:text="⌨ Terminal"/>
            <Button android:id="@+id/btnFiles" android:text="📁 Files"/>
            <Button android:id="@+id/btnCode" android:text="💻 Code"/>
            <Button android:id="@+id/btnLinux" android:text="🐧 Linux"/>
            <Button android:id="@+id/btnPackages" android:text="📦 Packages"/>
            <Button android:id="@+id/btnGit" android:text="🌿 Git"/>
            <Button android:id="@+id/btnAI" android:text="🤖 AI"/>
            <Button android:id="@+id/btnSettings" android:text="⚙ Settings"/>
        </LinearLayout>
    </HorizontalScrollView>
</LinearLayout>
