LinOx 1.0.0 tools

The application ships an ARM64 PRoot binary in:
app/src/main/assets/proot-aarch64-static

The Linux screen also supports selecting a compatible ARM64 PRoot binary from Android storage.

Do not place random executables here and expect them to run. PRoot must be an AArch64 ELF executable compatible with the device.
