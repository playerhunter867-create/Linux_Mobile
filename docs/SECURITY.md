# Security

LinOx Mobile does not promise unrestricted Linux privileges from a normal APK.

API keys are stored locally when configured and are never generated into source files.
No root exploit or security bypass is included.

Before enabling root/native functionality on a device, make a backup and understand the risks.
