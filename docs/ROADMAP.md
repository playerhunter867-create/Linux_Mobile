# LinOx Mobile roadmap

## 0.9 — current
- Stable ARM64 PRoot runtime
- Multi-distro OS manager
- OCI layer verification
- gzip/zstd layers
- whiteouts
- persistent rootfs selection
- modern dashboard
- terminal / editor / Git / workspace tools
- diagnostics commands

## 1.0
- better terminal rendering and keyboard shortcuts
- install queue with cancel/resume
- per-distro storage accounting
- backup/export of rootfs
- package search UI
- configurable terminal themes

## Future
- optional desktop/VNC sessions
- plugin system
- custom OCI registry support
- reproducible rootfs builds
- x86_64 support where a compatible execution strategy is available
